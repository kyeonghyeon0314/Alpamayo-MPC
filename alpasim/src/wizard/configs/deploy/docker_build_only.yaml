# @package _global_

defaults:
  - /stable_manifest/oss
  - _self_

defines:
  # The repo-relative resolver makes the path relative to the git repo root.
  filesystem: ${repo-relative:"data"}


services:
  driver:
    replicas_per_container: 1
    gpus: null

  sensorsim:
    replicas_per_container: 1
    gpus: null

  physics:
    replicas_per_container: 1
    gpus: null

  trafficsim:
    replicas_per_container: 1
    gpus: null

  controller:
    replicas_per_container: 1
    gpus: null


wizard:
  run_method: "NONE"
  dry_run: true
  validate_mount_points: false
  log_dir: "./generated_configs"

scenes:
  scene_ids: []
